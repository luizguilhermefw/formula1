import 'package:flutter/material.dart';

class CuriosidadesScreen extends StatelessWidget {
  const CuriosidadesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Curiosidades'),
    );
  }
}