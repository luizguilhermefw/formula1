import 'package:flutter/material.dart';

class CorridasScreen extends StatelessWidget {
  const CorridasScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Corridas'),
    );
  }
}